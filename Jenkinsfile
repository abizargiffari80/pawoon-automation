pipeline {
    agent any

  stages {
    stage('Prepare') {
        steps {
            echo "test echo saber test"
            bat 'dir'
            bat 'cd /Website'
            bat 'dir'
             }
          }
      }
}